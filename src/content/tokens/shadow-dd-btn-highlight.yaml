name: dd-btn-highlight
category: shadow
value: 'inset 0 1px 0 0 #ffffff2e'
css_var: '--shadow-btn-highlight'
description: DealDash Emerald — the "lit" top inner edge of every filled button. Dark mode `inset 0 1px 0 0 #ffffff1f`. Swap the --shadow-btn-* trio to restyle every button at once.
