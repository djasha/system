---
name: AutokitSellerBoxFunnel
description: BotBento Seller Box funnel — third AutoKit funnel through the weekly pipeline. Market-copper accent, price-tag mark, and a policy-era narrative grounded in verified 2026 marketplace rules.
category: layout
tags: [autokit, funnel, landing, seller]
tokens_used: [color.autokit-bg, color.autokit-amber, color.autokit-live, type.autokit-display, type.autokit-body, type.autokit-mono]
related: [autokit-bundle-themes]
status: beta
source_path: src/autokit/SellerBoxFunnel.astro
a11y_notes: Same three-layer contract as its siblings — works without JavaScript via the .js class gate, reduced motion respected in CSS, gsap.matchMedia and static fallbacks, theme set before first paint. The copper accent layer ships WCAG-checked -text values for both light and dark.
created: 2026-07-05
components_used: [autokit-button, autokit-pill, autokit-part-card, autokit-ticket, autokit-phone-chat, autokit-icons, autokit-motion-recipes]
---

## Prompt

Third re-skin of the funnel skeleton, proving the pipeline again: swap the accent layer to market copper, swap the mark to price tags (no lobster, no bento), and re-narrate for a compliance-era seller kit — the timely hook is real (Amazon's 75-character title rule, Etsy AI disclosure, the FTC review rule), so the page sells preparedness, not fear. Product facts come from the actual kit: 20 seller cards, a dated policy reference with sources, and the offline Listing Lab checker tool. Structure, motion, JSON-LD and accessibility identical to the siblings.

## Usage

- Live example at `/autokit/funnels/botbento-seller-box/` (noindex — production ships from botbento.com/seller-box when the kit is verified).
- **Status honesty**: the kit is in-the-works, and the page says so — marquee and CTAs read "ships when verified"; checkout is an `aria-disabled` placeholder; pricing ($24/$59/from $1,500) is DRAFT pending approval. The Setup Service mailto is real.
- Every policy claim on the page traces to the kit's `policy-quick-reference.md`, verified 2026-07-05 with sources.
- The three funnels together are the re-skin diff study: OpenClaw red → bento teal → market copper, same skeleton.

## Do / Don't

- **Do** keep policy claims dated and sourced — the credibility of this funnel IS the product.
- **Do** update the July 27 hook after the deadline passes (it becomes "the rules changed in July — you're covered").
- **Don't** enable indexing or wire checkout until the sellable ZIP exists and Diaa approves pricing.
- **Don't** use fear copy — the page sells being ready, never panic about suspensions.
